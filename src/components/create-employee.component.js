import React, { Component } from 'react';
import { View, Text } from 'react-native';
import { connect } from 'react-redux';

class CreateEmployee extends Component {
    state = {}

    render() {
        return (
            <View>
                <Text>CreateEmployee</Text>
            </View>
        );
    }
}
export default CreateEmployee;
